{
  "name": "mern-workouts-practice",
  "version": "1.0.0",
  "description": "react, nodejs and mongodb",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "start:frontend": "npm run start --workspace=frontend",
    "start:backend": "npm run dev --workspace=backend",
    "dev:api": "npm run build && npm run dev --workspace=backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces":[
    "frontend",
    "backend"
  ],
  "author": "",
  "license": "ISC"
}
